import React from 'react'
import { StyleSheet, Text, View, Button } from 'react-native'

const Exp = () => {
  return (
    <Button title='shreyash' onPress={() => {console.log('Shreyash pressed')}}/>
  )
}

export default Exp

const styles = StyleSheet.create({})